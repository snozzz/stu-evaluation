server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stu_evaluation?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: timegit111
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.evaluation.entity
  configuration:
    map-underscore-to-camel-case: true
    #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: StuEvaluation2026SecretKeyForJwtTokenGeneration
  expiration: 86400000

deepseek:
  api-key: ${DEEPSEEK_API_KEY:sk-placeholder}
  base-url: https://api.deepseek.com

logging:
  level:
    com.evaluation: info
